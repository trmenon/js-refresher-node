const print_duplicates = require('./duplicates');
const get_pairs = require('./sum-pair');
const find_common = require('./common');
const binary_search = require('./binary-search');

module.exports = {
    print_duplicates,
    get_pairs,
    find_common,
    binary_search
}